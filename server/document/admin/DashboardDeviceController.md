# Контроллер DashboardDeviceController `/admin/dashboard/device`

Панель-Устройства

## Методы

### [GET/sip `dashboard-device-sip-get`] Формирования списка устройств, без регистрации `/admin/dashboard/device/sip`
